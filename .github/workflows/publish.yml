name: publish

on:
  workflow_call:
    inputs:
      release_tag:
        description: "vX.Y.Z to publish; empty = edge :main build"
        required: false
        type: string
        default: ""
    secrets:
      DOCKERHUB_USERNAME:
        required: false
      DOCKERHUB_TOKEN:
        required: false
  workflow_dispatch:
    inputs:
      release_tag:
        description: "vX.Y.Z to publish; empty = edge :main test"
        required: false
        type: string
        default: ""
  # Build-only validation gate on the merge queue: the reusable builds both arches
  # WITHOUT pushing (catches arch-specific Python wheel breakage before main — issue #221).
  merge_group:

# Minimal top-level default; write scopes granted per-job only.
permissions:
  contents: read

jobs:
  policy:
    runs-on: ubuntu-24.04
    permissions:
      contents: read
    steps:
      - name: Harden the runner (Audit all outbound calls)
        uses: step-security/harden-runner@e14015d583714f6e62063499dc959a02595150a1 # v2.21.1
        with:
          egress-policy: audit

      - name: Checkout publication ref
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.release_tag || github.sha }}
          persist-credentials: false

      - name: Validate vulnerability exceptions
        run: python3 scripts/validate_trivy_exceptions.py

  codeql:
    permissions:
      contents: read
      packages: read
      actions: read
      security-events: write
    uses: rknightion/.github/.github/workflows/codeql.yml@27b0bd31dbe7bf238b89773d00d60ed5dfe187bb # v1.20.0
    with:
      languages: '[{"language":"python","build-mode":"none"},{"language":"actions","build-mode":"none"}]'
      ref: ${{ inputs.release_tag || github.sha }}
      category-suffix: /publish-gate
      fail-on-security-severity: high

  image:
    needs: [policy, codeql]
    permissions:
      contents: write
      packages: write
      id-token: write
      attestations: write
      security-events: write
    uses: rknightion/.github/.github/workflows/container-publish.yml@27b0bd31dbe7bf238b89773d00d60ed5dfe187bb # v1.20.0
    with:
      release-tag: ${{ inputs.release_tag }}
      helm-chart-path: charts/meraki-dashboard-exporter
      build-args: |
        PY_VERSION=3.14
        APP_VERSION=${{ inputs.release_tag || github.sha }}
        GIT_COMMIT=${{ github.sha }}
      dockerhub-login: true
      trivy-ignore-file: .trivyignore.yaml
    secrets:
      DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
      DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
