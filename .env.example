# Example environment variables for Meraki Dashboard Exporter
# Copy this file to .env and fill in your values

# Required: Your Meraki Dashboard API key
MERAKI_API_KEY=your_api_key_here

# Optional: Specific organization ID to export metrics for
# Leave empty to export metrics for all accessible organizations
MERAKI_EXPORTER_ORG_ID=

# Optional: Log level (DEBUG, INFO, WARNING, ERROR)
MERAKI_EXPORTER_LOG_LEVEL=INFO

# Optional: Device types to collect metrics for (comma-separated)
# Available: MS (switches), MR (access points), MV (cameras), MT (sensors)
# Default: MS,MR,MV,MT
# MERAKI_EXPORTER_DEVICE_TYPES=MS,MR,MV,MT

# Optional: OpenTelemetry configuration
MERAKI_EXPORTER_OTEL_ENABLED=false
MERAKI_EXPORTER_OTEL_ENDPOINT=http://localhost:4317
MERAKI_EXPORTER_OTEL_SERVICE_NAME=meraki-dashboard-exporter

# Docker Hub credentials (for pushing images)
# DOCKERHUB_USERNAME=yourusername
# DOCKERHUB_TOKEN=yourtoken
